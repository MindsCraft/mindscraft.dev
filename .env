# .env - Default environment variables (safe to commit)

# Public variables (accessible in the browser)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX# Replace with your actual ID in .env.local
NEXT_PUBLIC_SITE_URL=http://localhost:3000# Site URL for local development

# NextAuth configuration
NEXTAUTH_URL=http://localhost:3000# URL for NextAuth authentication
NEXTAUTH_SECRET=placeholder-secret-do-not-use-in-production  # Replace with a real secret in .env.local
